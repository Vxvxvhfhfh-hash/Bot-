{
  "name": "whatsapp-ai-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "python -m http.server 3000 || php -S localhost:3000 || node -e \"require('http').createServer((req,res)=>{require('fs').readFile('index.html',(e,d)=>res.end(d))}).listen(3000)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "socket.io-client": "^4.7.2",
    "axios": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}